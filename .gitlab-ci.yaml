stages:
    - deploy

# ---

deploy:image:
    before_script:
        - 'apk add --no-cache --no-progress ca-certificates curl'
    image:
        entrypoint: ['']
        name: alpine:latest
    only:
        - master
    script:
        - 'curl --request POST "https://cloud.docker.com/api/build/v1/source/267bcecf-4b7c-41ab-a660-18c3f888bae3/trigger/${DOCKER_CLOUD_TOKEN}/call/"'
    stage: deploy
